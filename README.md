# Repo Memory Skill

Additive memory router. Existing `napkin`, `theorist`, `evolver`, `seance`, and `qmd` remain available.

## Installation

```bash
ln -sfn ~/Developer/Personal/skill-repo-memory/skills/repo-memory ~/.claude/skills/repo-memory
ln -sfn ~/Developer/Personal/skill-repo-memory/skills/repo-memory ~/.codex/skills/repo-memory
```
